#ifndef NAIVE_NETWORK_NET_HPP
#define NAIVE_NETWORK_NET_HPP

namespace naive {
    class net {
    public:
        void initialize();
        void start();
        void shutdown();
    };
}
#endif //NAIVE_NETWORK_NET_HPP
